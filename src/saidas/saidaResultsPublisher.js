'use strict';

/**
 * Publica resultados ricos de uma saída no canal de resultados.
 * Stub — implementação completa depende de Discord embeds e canal designado.
 */
async function publishResults(_client, _saidaId) {
  // noop — implementação futura
  return null;
}

module.exports = { publishResults };
