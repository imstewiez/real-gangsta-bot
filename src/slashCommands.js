'use strict';

// O bot funciona por botões e modais. Mantemos este array vazio para o boot
// limpar comandos antigos registados no Discord através do PUT de guild commands.
const commands = [];

module.exports = { commands };
